# Copy to .env.render.local (git-ignored) and paste the real value.
# Render → ascend-db → Connections → External Database URL
RENDER_DATABASE_URL='postgresql://USER:PASSWORD@HOST.oregon-postgres.render.com/DBNAME'
