# Database Schema

PostgreSQL, managed via SQLAlchemy models (`backend/app/models/`) and Alembic
migrations (`backend/migrations/`).

## Entity-Relationship Overview

```
User ──< Job (creator_id)
User ──< Application (user_id)
Job  ──< Application (job_id)
Application ──1 Contract (application_id)
User ──< Payment (from_user_id / to_user_id)
User ──< Mentorship (mentor_id / mentee_id)
User ──< Message (sender_id / recipient_id)
Job  ──< Message (job_id, optional context)
```

## Tables

### `users`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| email | string, unique | |
| password_hash | string, nullable | null for OAuth-only accounts |
| first_name / last_name | string | |
| avatar_url | string | |
| bio | text | |
| user_type | enum | `founder`, `engineer`, `iec_worker`, `immigrant`, `admin` |
| status | enum | `active`, `inactive`, `suspended` |
| country / city | string | ISO country code + free text city |
| phone | string | |
| google_id / github_id | string, unique, nullable | OAuth identifiers |
| skills | JSON array | |
| languages | JSON array | |
| timezone | string | |
| visa_status | string | e.g. "IEC", "PR", "Work Permit" |
| visa_expiry | string (ISO date) | |
| email_verified | bool | |
| created_at / updated_at | datetime | |

### `jobs`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| creator_id | int, FK → users.id | |
| title / description | string / text | |
| company_name | string | |
| salary_min / salary_max | int (cents) | avoid float rounding |
| salary_currency | string(3) | ISO currency code |
| skills | JSON array | |
| experience_level | string | |
| job_type | enum | `full_time`, `part_time`, `contract`, `internship` |
| status | enum | `draft`, `open`, `closed`, `filled` |
| location_country / location_city | string | |
| remote_ok | bool | |
| visa_sponsorship | bool | |
| iec_friendly | bool | open to IEC working holiday participants |
| positions_available | int | |
| application_deadline | string (ISO date) | |

### `applications`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| job_id | int, FK → jobs.id | |
| user_id | int, FK → users.id | |
| status | enum | `applied`, `reviewing`, `interviewing`, `offered`, `rejected`, `hired`, `withdrawn` |
| cover_note | text | |
| notes | text | internal, hiring-side only |

Unique constraint (application-level, enforced in route logic): one
application per `(job_id, user_id)` pair.

### `contracts`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| application_id | int, FK → applications.id, nullable | |
| template_type | enum | `korea_engineer_canada_co`, `canada_engineer_korea_co`, `remote_contractor`, `full_time_domestic`, `part_time_gig` |
| status | enum | `draft`, `pending_signature`, `signed`, `cancelled` |
| terms | JSON | flexible per template — salary, dates, benefits, termination clause |
| docusign_envelope_id | string | |
| signed_at | datetime | |

### `payments`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| from_user_id | int, FK → users.id | |
| to_user_id | int, FK → users.id, nullable | null when recipient is off-platform |
| amount | int (cents) | |
| currency | string(3) | |
| payment_type | enum | `salary`, `contract_payment`, `remittance` |
| status | enum | `pending`, `processing`, `complete`, `failed`, `cancelled` |
| wise_transaction_id | string | |
| recipient_name / recipient_email | string | for off-platform recipients |
| notes | string | |

### `mentorships`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| mentor_id | int, FK → users.id | |
| mentee_id | int, FK → users.id | |
| status | enum | `requested`, `active`, `completed`, `declined` |
| notes | text | |

### `messages`
| Column | Type | Notes |
|---|---|---|
| id | int, PK | |
| sender_id / recipient_id | int, FK → users.id | |
| job_id | int, FK → jobs.id, nullable | conversation context |
| body | text | |
| read | bool | |

## Migrations

```bash
cd backend

# Generate a migration after changing models/
alembic revision --autogenerate -m "describe the change"

# Apply migrations
alembic upgrade head

# Roll back one migration
alembic downgrade -1
```

Always review autogenerated migrations before applying — Alembic doesn't
always detect enum changes or column renames correctly.
